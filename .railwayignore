# Railway should only deploy the API, not the full monorepo
# This forces Railway to focus on the /apps/api directory

# Ignore everything except the API
/*
!/apps/
apps/*
!/apps/api/

# Keep necessary root files for workspace detection
!package.json
!pnpm-workspace.yaml

# Ignore development and docs
*.md
.git/
.github/
.vscode/
.idea/
.env*
!.env.example