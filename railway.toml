# Railway configuration for SmartPlex API service
# Dockerfile is at repo root, builds from apps/api/

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
watchPaths = ["apps/api/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
