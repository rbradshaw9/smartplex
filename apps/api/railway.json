{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "poetry install --no-dev"
  },
  "deploy": {
    "startCommand": "poetry run uvicorn main:app --host 0.0.0.0 --port 8080",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}