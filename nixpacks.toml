[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = [
    "cd apps/api",
    "poetry install --no-dev"
]

[phases.build]
cmds = ["echo 'No build step required'"]

[start]
cmd = "cd apps/api && poetry run uvicorn main:app --host 0.0.0.0 --port $PORT"