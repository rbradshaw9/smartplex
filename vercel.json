{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.1 --activate && pnpm install",
  "buildCommand": "pnpm --filter @smartplex/web build",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/web/$1"
    }
  ]
}
